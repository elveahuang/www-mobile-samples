import React, { FC } from 'react';
import { Spin } from 'antd';

const Index: FC = () => {
    return (
        <div className="app text-center">
            <Spin />
        </div>
    );
};

export default Index;
